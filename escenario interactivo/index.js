const moneda = document.querySelectorAll(".moneda")
const contador = document.querySelectorAll("#contador")

moneda.forEach ( item =>{
    Item.style. filter = "grayscale(1)"
    Item.classList.add(salatarmoneda)
})
